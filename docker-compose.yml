version: '3.8'

services:
  netlab:
    build:
      context: .
      dockerfile: Dockerfile
    image: netlab-platform:latest
    container_name: netlab_server
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
